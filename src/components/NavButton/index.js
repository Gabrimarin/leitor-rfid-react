import React from 'react';
import { Container, Text } from './styles';
function NavButton({ selected = false }) {
    return (
      <Container selected={selected}>
        TESTE
      </Container>
    );
  }
  
  export default NavButton;